eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  cloud:
    config:
      server:
        git:
          uri: ${config.repo.uri:https://github.com/wanghuu/spring-cloud-config.git}
          searchPaths: respo