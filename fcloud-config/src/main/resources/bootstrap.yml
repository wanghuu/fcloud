spring:
  application:
    name: config-server 
  cloud:
    config:
      server:
        git:
          uri: ${config.repo.uri:https://github.com/wanghuu/spring-cloud-config.git}
          searchPaths: respo
    
server:
  port: 8888